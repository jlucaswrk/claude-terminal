{
  "name": "claude-terminal",
  "version": "0.1.0",
  "description": "Control Claude Code from WhatsApp - a terminal in your pocket",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.29",
    "@anthropic-ai/sdk": "^0.72.1",
    "hono": "^4.0.0",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
